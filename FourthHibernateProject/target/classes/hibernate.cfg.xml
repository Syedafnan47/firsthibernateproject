<?xml version="1.0" encoding="utf-8"?>
<hibernate-configuration>
    <session-factory>

        <!-- Database connection -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/hibernate</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">Syedafnan78647</property>

        <!-- Dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Logging / formatting -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Schema auto action (validate | update | create | create-drop) -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Annotated entity mapping (change package to your actual package) -->
        <mapping class="com.kod.FourthHibernateProject.Student"/>



    </session-factory>
</hibernate-configuration>
